apiVersion: v1
kind: Secret
metadata:
  name: payments-api-secret
  labels:
    app: payments-api
type: Opaque
stringData:
  # Plain text values; Kubernetes converts to base64 internally
  # JWT Key for authentication
  Jwt__Key: "chave-secreta-super-forte-123456"
  # Azure Service Bus connection string
  ServiceBus__ConnectionString: ""
  # Application Insights connection string (workspace-based for Log Analytics)
  APPLICATIONINSIGHTS_CONNECTION_STRING: ""
